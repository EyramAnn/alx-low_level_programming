#include <stdio.h>
/**
 * main -Entry point to print from
 * @argv: The character name of code to print
 * @argc: The character to print
 * Return: successful
 */
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
